# TAKARA 이미지 업로드 기능

이번 버전은 관리자 쿠지 등록 화면에서 이미지 파일을 직접 업로드할 수 있게 만든 버전입니다.

## 추가된 기능

- `/admin/kuji` 대표 이미지 파일 업로드
- 상품별 이미지 파일 업로드
- 업로드 후 이미지 URL 자동 입력
- 업로드 즉시 미리보기 표시
- 쿠지 등록 시 `kujis.image_url`, `prizes.image_url`에 저장
- `/kuji` 목록과 상세 페이지에서 대표 이미지 표시
- 결과/보관함에서 상품 이미지 표시가 가능한 구조 유지

## Supabase Storage

업로드 API는 `takara-images` 버킷을 사용합니다.

서버가 Service Role Key를 사용해 버킷을 자동 생성하려고 시도합니다. 자동 생성이 실패하면 Supabase에서 수동으로 아래 버킷을 만들어주세요.

- Bucket name: `takara-images`
- Public bucket: ON
- 허용 파일: JPG, PNG, WEBP, GIF
- 권장 최대 용량: 5MB

## 테스트 순서

1. 관리자 계정 로그인
2. `/admin/kuji` 접속
3. 대표 이미지 파일 선택
4. 이미지 미리보기 확인
5. 상품별 이미지 파일 선택
6. 쿠지 등록
7. `/kuji` 목록에서 대표 이미지 표시 확인
8. 상세 페이지에서 대표 이미지 표시 확인

## 주의

기존에 URL로 직접 입력하던 방식도 유지했습니다. 파일 업로드가 실패하는 경우 임시로 이미지 URL을 직접 넣어 테스트할 수 있습니다.
